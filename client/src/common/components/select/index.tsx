import select, { CustomSelectProps } from './select';
const Select = select;
export default Select;
export type CustomSelectProps = CustomSelectProps;
import { Option, Options } from 'react-select';
export type Option = Option;
export type Options = Options;